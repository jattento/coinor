---
status: accepted
---

# Build a native SwiftUI application

Coinor is implemented in Swift and SwiftUI, using AppKit bridges only for
macOS facilities that SwiftUI does not expose cleanly. It does not use
Electron, a browser application shell, or a cross-platform UI framework.
